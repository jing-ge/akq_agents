# 核心运行时依赖 — 与 pyproject.toml [project.dependencies] 保持一致。
# 可选组 (quant / dev) 见 pyproject.toml。
pydantic>=2.7
pyyaml>=6.0
pandas>=2.0
numpy>=1.26
apscheduler>=3.10
tenacity>=8.2
fastapi>=0.110
uvicorn>=0.29
jinja2>=3.1
pyarrow>=15
